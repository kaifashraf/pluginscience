# Responsive Design Rule

- **Always prioritize mobile-responsiveness:** Ensure all components, UI elements, and layouts are fully responsive across all screen sizes (mobile, tablet, desktop) using appropriate Tailwind breakpoints (`sm:`, `md:`, `lg:`, etc.) and flexible sizing (e.g., `vw`, `vh`, `%`, `flex`, `grid`). Never hardcode fixed sizes that break on smaller screens.
